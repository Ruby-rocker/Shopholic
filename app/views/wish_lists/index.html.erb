<h1>Listing Wish List</h1>

<table width="100%" border="2">
  <tr>
    <th>Product</th>
    <th>User</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @wish_lists.each do |wishlist| %>
  <tr>
    <td><%= wishlist.product_id %></td>
    <td><%= wishlist.user_id %></td>
    <td><%= link_to 'Show', wish_list %></td>
    <td><%= link_to 'Edit', edit_wish_list_path(wishlist) %></td>
    <td><%= link_to 'Destroy', cart, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Wish List', new_wish_list_path %>
