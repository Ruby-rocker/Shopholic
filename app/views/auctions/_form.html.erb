<%= form_for(@auction) do |f| %>
  <% if @auction.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@auction.errors.count, "error") %> prohibited this auction from being saved:</h2>

      <ul>
      <% @auction.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.hidden_field :user_id, :value => current_user.id %>
  </div>
  <div class="field">
    <%= f.label :name %> 
    <%= f.text_field :name,:value => current_user.fname %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
   <%= f.label :Product_Image %><br />
   <%= f.file_field :avatar %><br>
   <%= f.file_field :avatar %><br>
   <%= f.file_field :avatar %><br>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :reserved_price %><br />
    <%= f.number_field :reserved_price %>
  </div>
  <div class="field">
    <%= f.label :auction_ended %><br/>
    <%= f.text_field :auction_ended ,:id => 'datetimepicker2' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>